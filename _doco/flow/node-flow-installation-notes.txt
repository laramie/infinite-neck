sudo apt update && sudo apt upgrade -y
sudo apt install nodejs npm -y
node -v
npm -v
cd infinite-neck
npm init -y
npm install --save-dev jest

laramie@penguin:~/infinite-neck$ npm install --save-dev jest
npm WARN deprecated inflight@1.0.6: This module is not supported, and leaks memory. Do not use it. Check out lru-cache if you want a good and tested way to coalesce async requests by a key value, which is much more comprehensive and powerful.
npm WARN deprecated glob@7.2.3: Old versions of glob are not supported, and contain widely publicized security vulnerabilities, which have been fixed in the current version. Please update. Support for old versions may be purchased (at exorbitant rates) by contacting i@izs.me
npm WARN deprecated glob@10.5.0: Old versions of glob are not supported, and contain widely publicized security vulnerabilities, which have been fixed in the current version. Please update. Support for old versions may be purchased (at exorbitant rates) by contacting i@izs.me

npm audit
npm audit fixed

laramie@penguin:~/infinite-neck$ npm jest --version
9.2.0

# Running Jest tests in this setup: 
laramie@penguin:~/infinite-neck$ export INFINITE_NECK_VERBOSE=1
laramie@penguin:~/infinite-neck$ npx jest _tests/jest/song-load.test.js

=========
Knip
=========
npm init @knip/config

=========
  Updating node
=========
Here's what I did (20260302): 
 1372  sudo npm install -g n
 1374  sudo n lts
So now n is installed. Here's how to updated it, which updates to latest node:
sudo n latest


===========
 As of 202603/12
===========
laramie@penguin:~/pristine-cli-tools/cli-tools/bin/namespacer$ history | grep 'npm install'

 1994  sudo npm install -g n

 1965  npm install recast
 1985  npm install jscodeshift --save-dev
 1987  npm install @babel/core @babel/types @babel/generator
 1999  npm install pino

 Now, Claude says do the following, because --save-dev is for tools I use, others are to be attached to the project because, like the logger, everyone will need them.
 npm install recast
npm install jscodeshift --save-dev
npm install @babel/core @babel/types @babel/generator --save-dev
npm install pino

============
jsonpath-plus
============
npm install jsonpath-plus



