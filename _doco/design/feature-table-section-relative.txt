notetable.js :: colorSingleNotes()
   Line 475: calls gSong.removeNotePlayedFromTable(notePlayed, parentTableID);
   Line 483: calls gSong.getTableArrInCurrentSection(parentTableID);

Some things will display acording to the CurrentSection as master display:
    gSong.singleNoteOpacity 
    gSong.tinyNoteOpacity   
    gSong.amedNoteOpacity

Line 504: notetable.js :: replay()
    just calls once 
    var currSection = getCurrentSection();
        
    
    
Gotta handle this case directly:
    colorNote() ::
        notetable.js::322
            if (theColorClass == "noteClear"){  //color "noteClear" is hardcoded to mean actually clear/delete the note.
                    getCurrentSection().namedNotes[noteName] = {};
                    clearNamedNoteDivs(namedNoteDiv);
                    noteNameElements.find(".NoteDisplay").removeClass().addClass("NoteDisplay");;
                }    ...
Then, there are more for editing and playing:
    dropper()
    showHighlightsForBeat()
    doFill()
