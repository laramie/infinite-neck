

* = no direct call today or undoable

LoopSong
.
SongBegin (FirstSection)
SectionBegin
FirstBeat (default spot, but no direct call) *
NextBeat
PrevBeat
LastBeat *
NextSection
.
.
LoopSection *
.
LoopBeats
.
RestartSection * (plugins work because they work today when you do first section and you are already in first section) 
.
NextSection
.
RestartSong (FirstSection)
NextSongLoop * (skips ahead, fires OnSongEnd)
.
LastSection (first beat of)

Note:JumpToSongEnd is weird because you either get last beat (unplayed) or you get an infinitesimal between last beat and song end.  So NextSongLoop is preferred, and "play through to next song loop" (presumably firing every unplayed Section on the way) is not supported.

Also, loopBeats and LoopSection should be entered the same way loop Beats is handled.


m) "map spacerbar" Menu:

       * = Transport action not implemented

Song
  Loop
  Restart *
  Begin
  LastBeat *
  NextSongLoop *

Section
  First
  Next
  Prev
  Last
  Restart *
  LoopSection *

Beat
  First *
  Next
  Prev
  Last *
  LoopBeats


R) restart song (w/o DaCapo:OnSongEnd)
r) restart Section (begin Section / firstBeat)
z) reset song
Z) reset song hard
u) unset spacebar



Functions (make available through /run or /section/nav)
  gotoBeat n
  gotoSection n

new Song Looper events
  Restart
  Reset
  ResetHard (mostly for Transpose)

Later sprints
  Raise plugin in plugin menu by Select of saved keys
  Bury All
  Floating windows and panels
  ENTER key in edit boxes accepts like TAB does.
  Comment field for song, accepts html, displays in messages with /fc : file, comments.
  ArpeggioPlugin should have low String and high String limits
  ArpeggioPlugin should be able to select for flashcard mode those single notes that FillPlugin has placed.

TODO
  write demo songs for library that show plugin features, and companion doco.
  
MovePlugin design
  Veto "enable" menu because doesn't listen to events. (Is that the thing that makes PluginManager give you enable? So MovePlugin could simply not have events.)
 Algorithms 
  By shape
  By midi
    Drop
    Hide
    AllowOctaveSub
    AllowSub
  By chord notes
Will need min/max frets and strings. 

Will need multi toogle for notes types to include in move.

Need to have it out of the way so you can navigate to next section and hit apply. 

Ideal would be numbered macros that you bind to any plugin verb.
/fpmA
--> file, plugins, move, Apply
/fptA
--> file, plugins, transpose, Apply

Then
/m1 --> /fpmAx
/m2 --> /fmptAx

Then nav works because x is stored, thus menu closes. 

/snf (section, nav, first)
/m1
>
/m1
>
/m1

--> first three sections moved.






