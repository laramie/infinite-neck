# sprint-119 Iteration 6: Clip recordedNotes/beats

## Overview 
We have several ways of copying notes from tables, that is instruments, in infinite-neck to other tables, to other sections, etc. 

 
For all of these where we say must be in REC mode, or must not be in REC mode, if any of the conditions aren't met it's a no-op with a warning.

For all the places we say "warning" we mean the command result that ends up in the command-line dropdown.  For voluminous messages, we do showMessages, but all the warnings in this Iteration should fit in the command result.


## New section edit instrument clone

In addition to existing `/seic` :: "section > edit > instrument > clone",  we need to add `/seii` :: "section > edit > instrument > insert clone table into other section" > "INPUT> 1-based-section-number". This would be with an INPUT for the section number, similar to how we deal with it in `/rsg` including the prompt with `n+1`. If destination table is not empty in that section, or if section doesn't exist, this is a no-op with a warning.


## ClipPlugin new verbs

Then we need to add some verbs to ClipPlugin.  At the main level `/fpc` we would start with a menu item called `r) recorded notes` and then drop into there. Next we would see copy or paste, and so on, as shown in "Proposed Menu Structure".

Selecting one of `c) copy` > `a) all beats` would copy all notes in all the recorded beats.

Selecting one of `c) copy` > `c) current beat` would copy all notes in the current beat.

There is no flow for selecting the note type. The note type menu-item from `/fpci` is ignored.  In this whole new clip plug-in flow we are describing in this Iteration 6, we grab all note types: there is no no type filtering. 

The user would then traverse to the section and beat location where they wanted to begin pasting. 

The user would select REC mode or not with the button on the transport. 

For this iteration, we are only doing tables with same Lineage, so it could be a different table, but n-strings and midiPitiches must line up. This means that we are not doing MIDI Paste, and that styleNum:row:col lines up for same pitches.  This means they would have come back in through ClipPlugin and changed Instrument.  That is part of the valid state when comming back into the paste in `/fpcrp`

The User would drop into `/fpcrp` and select menus for the following scenarios below.

At this point the user is ready to paste beats either in REC mode or not REC mode, and they are at the current section, the current beat, and have a destination table selected that is compatible with the Lineage of the source table where the current Clip is from.  

We now consider that the user has a set of beats in the Clip memory and each beat contains recorded notes. We'll call this the source set. 

Remember that the User was in charge of deciding if the source set was one beat, or many beats.  However, the actual number, if not "current beat", was actually the number of beats in the source Section.

The source set could have, say, four beats with multiple types and instances of notes each.  

So we want to support several types of scenarios around number of beats in the source set and the number of beats in the destination. 

### First scenario. "play beats into current" when source set has one beat

For this scenario REC must be on. 

Take the only beat from the source set and play it into the current beat overwriting notes of the same note type and row:col that are already there, replacing notes upon collision. This is important because the new note might have a different color or roll. We want the new note.  

### Second scenario. "play beats into current"

For this scenario REC must be on. 

Starting with the current beat in the destination, that is the current section, take the first beat of the source set and play the beats into the current beat overwriting collisions, and advancing beats so that one beat from the source set hits one beat in the destination set.  Both advance one until we exhaust the possibility of having a source match up with a destination. When we run out of source set beats we stop. If we run out of destination beats we stop -- we don't create any new beats.

Examples, showing one note per beat, and current beat with one note F as *F* to start:

source (4 beats):  A B C D
destination (3 beats): E *F* G
result (3 beats): E *FA* GB

source (4 beats):  A B C D
destination (1 beat): *F*
result (1 beat): *FA*

source (4 beats):  A B C D
destination (4 beat): *F* G A B
result (4 beats): *FA* GB AC BD

source (1 beat):  A
destination (4 beat): E *F* G B 
result (4 beats): E *FA* G B



destination: 
### Third scenario. "squeeze beats into current beat"

For this scenario REC must be on. 

Take all the beats in the source set and play them into the current beat replacing any notes that are collisions.

Examples: 

source (4 beats):  A B C D
destination (3 beats): E *F* G
result (3 beats): E *FABCD* G

### Fourth scenario "add all beats"

For this scenario REC must be on. 

For this scenario, all beats in the source set will end up in the destination section as new beats. We start after the current beat in the destination, and so insert/append. No overwriting occurs because we either insert after the current beat, or we are spending after the last beat.

source (4 beats):  A B C D
destination (3 beats): E *F* G
result (7 beats): E *F* A B C D G

### Fifth scenario. "insert all beats"

For this scenario REC must be on. 

Same as "add all beats" but instead of after the current beat we start *before* the current beat.

source (4 beats):  A B C D
destination (3 beats): E *F* G
result (7 beats): E A B C D *F* G

### Non-scenario. "grab some beats"

For a scenario where User wants to grab just 3 beats from current section with 5 beats, he will instead copy or clone beats into new section, delete beats there, preview, then copy all. This is a separate, manual flow we won't deal with.

### Sixth scenario. "flatten beats into playedNotes"

For this scenario REC must be off. 

Play all notes in the source set, that is to say, all the beats in that source set, in order, into the current section but not in REC mode, with Multi highlight last. Therefore they all become played notes. 

For "flatten beats into playedNotes", the order is important. First SingleNotes, TinyNotes, and Bends, then Pitch then Multi. This way, Multi wins over Pitch, although both Midi types (Pitch and Multi) will be lost on navigation etc.  But for the moment after the paste, the user can see Pitch if present, or Multi if present, and if both, then they say Multi if present. Result message should succinctly mention that Highlights will be lost on navigation if they are present.  Message format: `Pitch/Multi are temporary` but using the sum of which were included, e.g. `Pitch`, `Pitch/Multi`, `Multi`.  The result line may get long for the dropdown, but we are going to clean these up in a later sprint.

source (4 beats):  A B C D
destination (3 beats): E *F* G
result (3 beats):  E *F* G
BUT: playedNotes: [A,B,C,D]


## Proposed Menu Structure

Menu structure is shown indented, with format `t) caption` where t is the trigger, and caption is the caption text, and t is bolded in caption. For existing lines, we are not showing the trigger, so they would just have the format `clear clipped notes`.

In the examples, when you see array brackets, it means a default filled in by the system, and we may show example text, for example: 
`    Instrument [P46_1]`
because P46_1 was the value that was true when this menu was captured.

Some lines start off with path, shown as `/fpc` which would mean `/` `file` `plugins` `clip` in the command-line menu.

In all menus, `e<b>x</b>it` is ommitted but would be present because it is constructed by the system.

Here is what we have today:
```
/fpc
    Bury
    Instrument [P46_1]
    C copy
    X cut
    V paste [12:tiny-4-0107]
    MIDI Paste [14:tiny-4-0107]
    L copy Listened notes
    automatic [true]
    overwrite [true]
    include [n:0,s:0,t:4,b:0,f:0]
    clear clipped notes
    help
```

Here is what we'll have after implementation of sprint-119 Iteration 6:
```
/fpc
    Bury
    Instrument [P46_1]
    C copy
    X cut
    V paste [12:tiny-4-0107]
    MIDI Paste [14:tiny-4-0107]
    L copy Listened notes
    automatic [true]
    overwrite [true]
    include [n:0,s:0,t:4,b:0,f:0]
    r) recorded notes
        c) copy
            status: [tblname:sectionNum:beatnum]
            c) current beat
            a) all beats
        p) paste
            status: [tblname:sectionNum:beatnum]
            a) add all beats
            i) insert all beats
            p) play beats into current
            s) squeeze beats into current beat
            f) flatten beats into playedNotes
    clear clipped notes
    help

```  

The special line `status: [tblname:sectionNum:beatnum]` is informational, and has no trigger or bolded letter.  You can see an example that would apply if you look in `/fm` where we show the current mapping of the spacebar in the first menu item `current: []`

## Request

Copilot, please provide analyisis and an implementation plan with questions for us before we approve coding changes in:
  [sprint-119 Iteration 6 implementation plan](119-it6-implementation-plan.md)

# sprint-119 Iteration 6 : Questions answered

Here we answer questions posed in the implementation plan.

1. **Clip storage:** Should recorded-notes clips be Graveyard-backed like current `ClipPlugin` clips, or should this new recorded-notes flow use only an in-memory latest clip?  ANSWER: Graveyard-backed proposal is perfect.

2. **Overwrite setting:** Should recorded-note paste always replace collisions as the sprint text says, ignoring the existing `overwrite` property, or should `overwrite=false` skip collisions? ANSWER: overwrite should not apply to this menu level.  We may want it later, and may even want MIDI Paste handling, but both are out of scope.  The main use case is that recorded notes are not handled by ClipPlugin, and are opaque to the User unless they do beat looping or section looping.  So when they have a Section they want to copy recorded notes from, most times they are adding the notes they see looping in another section that work as a set, and simply want them in another section.  Esoteric merge/repair scenarios are not envisioned or supported.

3. **Flatten included styles:** For `flatten beats into playedNotes`, should `Fingering` and `Named` recorded notes be ignored, included, or warned as unsupported? The sprint explicitly names Single, Tiny, Bend, Pitch, and Multi. ANSWER: We always forget about Fingering.  It should be included!  NamedNotes should be excluded, mostly because they are impossible to record into recordedNotes by design.  In the case that one leaks in or is edited in via User-supplied songfile, they should not be attempted.  Therefore they are specifically excluded.

4. **Empty source beats:** When copying all beats, should empty source beats be preserved so paste inserts/rests keep timing, or should empty beats be dropped? This plan recommends preserving them. ANSWER: Preserve empty beats--they are definitely meaningful.  Playing on beats 2 and 4 is a different musical style than playing on beats 1 and 3, and most software we know of implements this as empty beats.

5. **`/seii` destination emptiness:** Should "destination table is not empty" mean only the same `tableID` in the destination section is non-empty, or any table in the destination section is non-empty? This plan assumes same `tableID` only. ANSWER:  The idea is *not* to hose existing table data in destination Section.  Section 1 has table1 data and table2 data.  Section 2 has table 1 data.  We can clone Section1, but this clones Section1:table1 data as well, therefore we can't wholesale copy Section1:table2 data into Section2:table2 location.  We can copy/paste, but we wanted a more absolute, clean path.  However, if Section2:table2 has any kind of real note in it, we don't want to hose it.  It is for when that Section and that table have no notes.

6. **`/seii` allowed destination:** Should `/seii` allow inserting into the current section if the selected table is missing/empty there, or should it require a different section? ANSWER:  The source is the current Section, so this should be dis-allowed.  Good catch! 

7. **Lineage strictness:** Is exact `midiPitches` array equality required in addition to same lineage and `nStrings`, or is same lineage enough? This plan recommends exact equality to satisfy "cells line up". ANSWER: We may be giving the current check for lineage too much specificity.  For this Iteration 6, however, we do want the specificity that we are not doing MIDI paste, so midiPitches must line up.  We edited the places in our doc to get rid of "cell".  

8. **Batch insertion helper location:** Is it acceptable to add a small general-purpose beat insertion helper to [Song.js](Song.js), or should all beat insertion remain inside `ClipPlugin` for feature isolation? ANSWER:  Yes, this seems worthy to add to the Song. 

9. **Status line format:** Is `status: [P46_1:1:2]` acceptable, or should the displayed section and beat labels be more verbose, such as `status: [P46_1:Section 1:Beat 2]`? ANSWER: `status: [P46_1:1:2]` is perfect and succinct.

10. **Result verbosity:** Are compact result strings such as `played 2/4 beats: added 5, overwritten 1` acceptable for the command dropdown? ANSWER: Yes.  The other examples in the implementation plan are also good.

Additionally, we approve: 
- Use Graveyard-backed recorded-notes clips with `clipKind: "recordedNotes"`.
- Preserve empty beats when copying all beats.
- Ignore existing note-type include filters for recorded-notes copy/paste.
- Ignore the existing `overwrite` property and always replace recorded-note collisions.
- Require same lineage, same `nStrings`, and exact `midiPitches` equality for recorded-note paste.
- Add a small `Song` helper for `/seii` and likely a second tested helper for batch beat insertion.
- Keep all warnings short and in the command result line.

# sprint-119 Iteration pre-6
## Question: 
We are disturbed that REC mode lives entirely in jQuery UI.  We had forgotten about that.  We do *not* want it persisted or part of the schema (default state on load should always be OFF--it is a surprising state to be in on opening a song).  Having said those caveats, we would appreciate a more MVC approach.  Our app is not nearly MVC, but you can see many examples where we extract these kinds of things to the Song, and use infinite-neck.js or NoteTableController.js as Controllers.  So moving it to a run-time location in Song or some place similar would be a good thing.  We have a few gee-gaws that depend on this state, for example: 
```
<span class="instrumentSectionMark"><span class="RecordDot RecordButton ButtonOn"></span>§</span>
```
so they should be treated as proper views, and the current system of CSS class broadcast has been working well, so we'd opt for that rather than a new EventBus event.  If this makes sense as an Iteration before Iteration 6, please advise us.

## Response
Yes, approve "Iteration pre-6" to fix this.
We'll test that the LooperLights respond to REC and return to Iteration 6.

# sprint-119 Iteration 6 ready for coding

Iteration pre-6 has been implemented and tested now, and it all works, and persisting and re-loading and opening the persisted song brings us up in the right REC state (off) and recorded notes behaving right, and LooperLights showing REC properly.

One small item we noticed in implementation plan is now fixed in our document, but persists in implementation plan: 
Incorrect: 
```
r) recorded notes
		c) copy
				status: [tblname:sectionNum:beatnum]
				c) current beat - copy
				a) all beats - copy
```

Correct: 
```
r) recorded notes
		c) copy
				status: [tblname:sectionNum:beatnum]
				c) current beat
				a) all beats
```

Our answers to implementation plan questions are above.

Please proceed with coding Iteration 6 now that Iteration pre-6 passes.

# sprint-119 Iteration 7: MIDI Paste

Testing is going well.

We are now ready to include menu-item `/fpcrpm` "m) midi paste [true]", which is an org.dynamide.toggle, defaulting to `true`.  It is mostly an obvious replacement, and for identical source and dest instruments should be the same result as the current paste algorithm.  However, this gives us a solid way to test this, and it also allows the User a way to protect against pasting to different tunings if he wants to guard against that.

Location: 
```
status: [P46_1:1:1]
midi paste [true]
add all beats
insert all beats
play beats into current
squeeze beats into current beat
flatten beats into playedNotes
```

NOTE: it is imporantant not to undo our change in menu.js at `/seii`

```
                {
                  "caption": "<b>i</b>nsert clone [${sectionEditInstrumentBaseID}] into Section",
                  "trigger": "i",
                  "action": "sectionEditInstrumentInsertIntoSection",
                  "vars": [
                    "sectionEditInstrumentBaseID"
                  ],
                  "input": {
                    "type": "input",
                    "caption": "section number (1-${sectionEditNextSectionCardinal})",
                    "default": "${sectionEditNextSectionCardinal}",
                    "datatype": "Number",
                    "id": "value"
                  },
                  "popOnBang": true
                },
```
OUR CHANGE:
```
                  "input": {
                    "type": "input",
                    "caption": "section number (1-${sectionCount})",
                    "default": "",
                    "datatype": "Number",
                    "id": "value"
                  },
```
Because there is not a sectionEditNextSectionCardinal if you aren't appending.  This menu
item requires an existing Section, so that won't work for Next Section Cardinal.
Also, the default prompt is confusing.  There's already validation, and trying to cook 
the default with the validation would be too complicated.  Leave it empty, and let the User type in the Section number.




