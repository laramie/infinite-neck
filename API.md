<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

*   [Page][1]
    *   [Parameters][2]
    *   [whenAllWidgetsLoaded][3]
*   [WidgetLoader][4]
    *   [loadWidget][5]
        *   [Parameters][6]
    *   [fetchChunks][7]
        *   [Parameters][8]
*   [tagonomy/widgets/include/include][9]
*   [makeWidget][10]
    *   [Parameters][11]
*   [makeWidget][12]
    *   [Parameters][13]
*   [makeWidget][14]
    *   [Parameters][15]
*   [makeWidget][16]
    *   [Parameters][17]
*   [IncludeWidget][18]
    *   [Parameters][19]
    *   [grabContents][20]
        *   [Parameters][21]
    *   [build][22]
*   [tagonomy/widgets/org.dynamide.gallery.widget][23]
*   [WidgetDefaults][24]
    *   [Properties][25]
*   [GalleryWidget][26]
    *   [Parameters][27]
    *   [build][28]
    *   [repaint][29]
    *   [resize][30]
        *   [Parameters][31]
    *   [addImage][32]
        *   [Parameters][33]
    *   [registerEvents][34]
*   [tagonomy/widgets/org.dynamide.Widget][35]
*   [Widget][36]
    *   [Parameters][37]
    *   [Properties][38]
    *   [getClassKey][39]
    *   [load][40]
        *   [Parameters][41]
    *   [loaded][42]
        *   [Parameters][43]
    *   [registerCSS][44]
        *   [Parameters][45]
    *   [grabContents][46]
        *   [Parameters][47]
    *   [build][48]
    *   [repaint][49]
    *   [registerEvents][50]
    *   [makeClassKey][51]
        *   [Parameters][52]
*   [tagonomy/widgets/partial/partial][53]
*   [PartialWidget][54]
    *   [Parameters][55]
    *   [build][56]

## Page

Represents a web page html file that has widget elements: <widget id="theID" type="org.dynamide.WidgetOne" />
Knows how to find those widgets, load them,
and insert their build() result into the slot of the widget tag.
Each widget.id is provided by the designer of the html file,
who can then expect that after loading, each widget may be found by id in the DOM and also
reference its javascript class registered by id from javascript.

Cleanup is by jQuery.remove() which detaches from DOM and allows garbage collection,
but does not call widget.destroy() or anything fancy.  Therefore, until any fancy registry is in place,
store data on data- attributes.

### Parameters

*   `widgetLoader` &#x20;

### whenAllWidgetsLoaded

Returns a Promise that resolves when all widgets have been loaded and built.
Usage: await page.whenAllWidgetsLoaded();

## WidgetLoader

### loadWidget

Dynamically loads a widget module and initializes it.

#### Parameters

*   `widgetPath` &#x20;
*   `id` &#x20;
*   `dataAttributes` &#x20;
*   `page` &#x20;
*   `jWidget` &#x20;

### fetchChunks

Widget subclasses should call this in load() to pull in resources, but
can also just set this.includes instead.

#### Parameters

*   `items` &#x20;
*   `loadedFunction` &#x20;

## tagonomy/widgets/include/include

## makeWidget

Factory function to create an IncludeWidget.

### Parameters

*   `type` **[string][57]** The widget type.
*   `id` **[string][57]** The widget id.
*   `dataAttributes` **[Object][58]** Data attributes from the widget tag.
*   `page` **[Object][58]** The page context.

Returns **[IncludeWidget][18]**&#x20;

## makeWidget

Factory function to create a GalleryWidget.

### Parameters

*   `type` **[string][57]** The widget type.
*   `id` **[string][57]** The widget id.
*   `dataAttributes` **[Object][58]** Data attributes from the widget tag.
*   `page` **[Object][58]** The page context.

Returns **[GalleryWidget][26]**&#x20;

## makeWidget

Factory function to create a Widget.

### Parameters

*   `type` **[string][57]** The relative path to the concrete Widget subclass. Used to determine the widget type and CSS key.
*   `id` **[string][57]** The id of the <widget> element, and subsequently the id of any root element containing the result of build().
*   `dataAttributes` **[Object][58]** A dictionary of all the "data-\*" attributes on <widget> in the exact form found (e.g., dataAttributes\["data-includes"]).
*   `page` **[Object][58]** A live reference to the [Page][1] instance that represents the web page containing this widget.

Returns **[Widget][36]**&#x20;

## makeWidget

Factory function to create a PartialWidget.

### Parameters

*   `type` **[string][57]** The widget type.
*   `id` **[string][57]** The widget id.
*   `dataAttributes` **[Object][58]** Data attributes from the widget tag.
*   `page` **[Object][58]** The page context.

Returns **[PartialWidget][54]**&#x20;

## IncludeWidget

**Extends Widget**

IncludeWidget class.

Extends [Widget][36].

The IncludeWidget behaves just like the PartialWidget, except that the includes may be specified by a text node within the widget element, rather than the "data-includes" attribute, for readability.

Any includes specified will be processed:

*   If "css" is present, its content will be appended to the HEAD element.
*   If "html" is present, its content will be appended to a SPAN element and returned from [IncludeWidget#build][59], replacing the original \<widget> element.

If the \<widget> element contains a text node, it will be parsed as JSON and expected to be of the following structure:

    [
      {"id":"html","url":"include-test.shtml"},
      {"id":"css","url":"include-test.css"}
    ]

where each object specifies the id for the included chunk and the url to fetch.

Any child nodes or text content within the widget tag will be ignored after parsing.

The [IncludeWidget#build][59] method returns `undefined` if no "html" include is specified,
so the widget will emit no block element and disappear from the DOM, but any "css" will still be appended to HEAD.

### Parameters

*   `type` **[string][57]** The widget type.
*   `id` **[string][57]** The widget id.
*   `dataAttributes` **[Object][58]** Data attributes from the widget tag.
*   `page` **[Object][58]** The page context.

### grabContents

Grabs and parses the contents of the widget element as JSON to set includes.
The expected structure is an array of objects with "id" and "url" properties.
After parsing, the element is emptied.

#### Parameters

*   `jElement` **jQuery** The jQuery element representing the widget.

### build

Builds the widget's DOM representation.
If "html" is included, returns a SPAN element containing the HTML.
If not, returns undefined (widget disappears from DOM, but CSS is still applied).

Returns **(jQuery | [undefined][60])**&#x20;

## tagonomy/widgets/org.dynamide.gallery.widget

## WidgetDefaults

Default configuration for GalleryWidget.

Type: [Object][58]

### Properties

*   `type` **[string][57]** The widget type.
*   `includes` **[Array][61]<[Object][58]>** Default includes for CSS and HTML.
*   `events` **[Array][61]<[string][57]>** Events handled by this widget.

## GalleryWidget

**Extends Widget**

GalleryWidget class.

Extends [module:tagonomy/widgets/org.dynamide.Widget\~Widget][62].

<p>The GalleryWidget implements {@link GalleryWidget#build} to create a SPAN that shows image files pulled in via the "html" chunk.
It manipulates the presentation of these images with data attributes "data-width" and "data-height".
</p>

<p>The GalleryWidget provides its default includes in WidgetDefaults, however this.includes may be modified.
</p>

<p>The GalleryWidget implements {@link GalleryWidget#registerEvents} to register the events listed in WidgetDefaults,
using its handler methods {@link GalleryWidget#resize} and {@link GalleryWidget#addImage}, which you can override or call.
</p>

Constructs a GalleryWidget instance.

### Parameters

*   `type` **[string][57]** The widget type.
*   `id` **[string][57]** The widget id.
*   `dataAttributes` **[Object][58]** Data attributes from the widget tag.
*   `page` **[Object][58]** The page context.

### build

Builds the widget's DOM representation.
Creates a SPAN element and populates it with the HTML chunk.
If "data-width" and "data-height" attributes are present, sets the size of the first image.

Returns **jQuery** The root element for the widget.

### repaint

Called to repaint or update the widget's DOM representation.
Subclasses may implement this as needed.

### resize

Handler for the "org.dynamide.gallery:Resize" event.
Override or call this method to handle resize events.

#### Parameters

*   `data` **[Object][58]** Event data.

### addImage

Handler for the "org.dynamide.gallery:AddImage" event.
Override or call this method to handle add image events.

#### Parameters

*   `data` **[Object][58]** Event data.

### registerEvents

Registers event handlers for the events listed in WidgetDefaults.
Uses [GalleryWidget#resize][63] and [GalleryWidget#addImage][64] as handlers.

## tagonomy/widgets/org.dynamide.Widget

## Widget

Widget class.

The Widget class is a concrete class that will handle includes in the <widget> element's "data-includes" attribute,
which will append a CSS style tag to the HEAD tag, but does not implement [Widget#build][65].
Subclasses are expected to implement [Widget#build][65] if they want to have an element be in the DOM in the BODY tag.

Widget ignores any child nodes or text node within the widget element, but subclasses may grab these by implementing [Widget#grabContents][66]
before those nodes are thrown away.

Subclasses should not hold onto any jQuery or DOM elements after returning from methods.

The [Widget#build][65] method is expected to return:

*   `undefined` if no element should be appended to the DOM
*   a jQuery element such as <span> or <div> that will preserve any elements within it, and be available for the Widget subclass to manipulate later.
    This element will have its "id" attribute set to the "id" of the <widget> element as authored in the page.

After construction, loading, and DOM insertion, you may respond to any [EventBus][67] messages you have registered during [Widget#registerEvents][68].

constructor()

Constructs a Widget instance.

Deals with includes by looking at "data-includes" attribute on the widget element.
If present, creates a dictionary of this shape:

    "includes": [
     {
       "id": "css",
       "url": "widgets/org.dynamide.gallery.css"
     },
     {
       "id": "html",
       "url": "widgets/org.dynamide.gallery.shtml"
     }
    ]

and passes that to WidgetLoader.fetch... which returns `chunks` which is of this shape:

    "chunks": {
      "css": {
        "id": "css",
        "url": "widgets/org.dynamide.gallery.css",
        "text": ".gallery {display: inline-block; background-color: blue;}"
      },
      "html": {
        "id": "html",
        "url": "widgets/org.dynamide.gallery.shtml",
        "text": "<p class=\"gallery\">....</p>"
      }
    }

### Parameters

*   `type` **[string][57]** The relative path to the concrete Widget subclass. Use [Widget.makeClassKey][69] or [Widget#getClassKey][70] for CSS keys.
*   `id` **[string][57]** The id of the <widget> element and any root element containing the result of build().
*   `dataAttributes` **[Object][58]** Dictionary of all "data-\*" attributes on <widget> (e.g., dataAttributes\["data-includes"]).
*   `page` **[Object][58]** Reference to the [Page][1] instance containing this widget.

### Properties

*   `chunks` **[Object][58]** Dictionary of loaded resource chunks (e.g., "css", "html").
*   `includes` **[Object][58]** Dictionary of resources to fetch, loaded automatically from "data-includes" (e.g., "css", "html").

### getClassKey

Gets the CSS class key for this widget instance.

Returns **[string][57]** The CSS class key.

### load

Called when the WidgetLoader has constructed this Widget concrete subclass.
At this point, "this" and any methods you have defined are accessible, but any includes or resources have not been fetched yet.
This is your opportunity to request these resources with widgetLoader.fetchChunks().
Generally, you do not need to override this method; simply ensure this.includes is properly filled out.
If you do override this method, follow the pattern in Widget.load() and pass your loaded() function pointer with an arrow function so "this" is bound.
When those resources have been loaded, [Widget#loaded][71] will be called.
If you do not call super.loaded(widgetLoader), you'll break the includes, but also your
loaded() function will never be called, which breaks how Page loads all Widgets,
causing it to never return from the Promise in whenAllWidgetsLoaded().

#### Parameters

*   `widgetLoader` **[Object][58]** The WidgetLoader instance.

### loaded

Called when fetch() for resources completes.
All the resources you requested in this.includes are present in the dictionary chunks, with standard keys being "css" and "html".
Example:

    {
      "css": {
        "id": "css",
        "url": "partial-test-standalone.css",
        "text": ".partial-test-standalone { ... }"
      }
    }

This method is expected to call this.page.widgetLoaded(this) so that Page can call widget.registerCSS(), widget.build(), and widget.registerEvents().

#### Parameters

*   `chunks` **[Object][58]** Dictionary of loaded resource chunks.

### registerCSS

Registers CSS rules by appending a <style> element to the given head element.

#### Parameters

*   `jHeadElement` **jQuery** jQuery object for the <head> element to append the style to.

### grabContents

This is the one chance for the widget to reliably get the contents from the page,
after this we may remove the widget element and replace it with the results of build()
with the widget.id as that element.

Widget ignores any child nodes or text node within the widget element, but subclasses may grab these by implementing this method before those nodes are thrown away.
Subclasses should not hold onto any jQuery or DOM elements after returning from this method.

#### Parameters

*   `jElement` **jQuery** The jQuery element representing the widget.

### build

Subclasses are expected to implement this method if they want to have an element be in the DOM in the BODY tag.

The build() method is expected to return:

*   `undefined` if no element should be appended to the DOM
*   a jQuery element such as <span> or <div> that will preserve any elements within it, and be available for the Widget subclass to manipulate later.
    This element will have its "id" attribute set to the "id" of the <widget> element as authored in the page.

Subclasses should not hold onto any jQuery or DOM elements after returning from this method.

Returns **(jQuery | [undefined][60])**&#x20;

### repaint

Called to repaint or update the widget's DOM representation.
Subclasses may implement this as needed.

### registerEvents

Called to register any event handlers for this widget.
After construction, loading, and DOM insertion, you may respond to any [EventBus][67] messages you have registered here.
Subclasses may implement this as needed.

### makeClassKey

Converts a widget type (path) to a CSS class key.

#### Parameters

*   `type` **[string][57]** The widget type (path).

Returns **[string][57]** The CSS class key.

## tagonomy/widgets/partial/partial

## PartialWidget

**Extends Widget**

PartialWidget class.

Extends [Widget][36].

Any includes in the "data-includes" attribute will be processed:

*   If "css" is present, its content will be appended to the HEAD element.
*   If "html" is present, its content will be appended to a SPAN element and returned from [PartialWidget#build][72], replacing the original \<widget> element.

Any child nodes or text content within the widget tag will be ignored.

The [PartialWidget#build][72] method returns `undefined` if no "html" include is specified,
so the widget will emit no block element and disappear from the DOM, but any "css" will still be appended to HEAD.

### Parameters

*   `type` **[string][57]** The widget type.
*   `id` **[string][57]** The widget id.
*   `dataAttributes` **[Object][58]** Data attributes from the widget tag.
*   `page` **[Object][58]** The page context.

### build

Builds the widget's DOM representation.
If "html" is included, returns a SPAN element containing the HTML.
If not, returns undefined (widget disappears from DOM, but CSS is still applied).

Returns **(jQuery | [undefined][60])**&#x20;

[1]: #page

[2]: #parameters

[3]: #whenallwidgetsloaded

[4]: #widgetloader

[5]: #loadwidget

[6]: #parameters-1

[7]: #fetchchunks

[8]: #parameters-2

[9]: #tagonomywidgetsincludeinclude

[10]: #makewidget

[11]: #parameters-3

[12]: #makewidget-1

[13]: #parameters-4

[14]: #makewidget-2

[15]: #parameters-5

[16]: #makewidget-3

[17]: #parameters-6

[18]: #includewidget

[19]: #parameters-7

[20]: #grabcontents

[21]: #parameters-8

[22]: #build

[23]: #tagonomywidgetsorgdynamidegallerywidget

[24]: #widgetdefaults

[25]: #properties

[26]: #gallerywidget

[27]: #parameters-9

[28]: #build-1

[29]: #repaint

[30]: #resize

[31]: #parameters-10

[32]: #addimage

[33]: #parameters-11

[34]: #registerevents

[35]: #tagonomywidgetsorgdynamidewidget

[36]: #widget

[37]: #parameters-12

[38]: #properties-1

[39]: #getclasskey

[40]: #load

[41]: #parameters-13

[42]: #loaded

[43]: #parameters-14

[44]: #registercss

[45]: #parameters-15

[46]: #grabcontents-1

[47]: #parameters-16

[48]: #build-2

[49]: #repaint-1

[50]: #registerevents-1

[51]: #makeclasskey

[52]: #parameters-17

[53]: #tagonomywidgetspartialpartial

[54]: #partialwidget

[55]: #parameters-18

[56]: #build-3

[57]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String

[58]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object

[59]: #includewidgetbuild

[60]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/undefined

[61]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array

[62]: module:tagonomy/widgets/org.dynamide.Widget~Widget

[63]: #gallerywidgetresize

[64]: #gallerywidgetaddimage

[65]: #widgetbuild

[66]: #widgetgrabcontents

[67]: EventBus

[68]: #widgetregisterevents

[69]: #widgetmakeclasskey

[70]: #widgetgetclasskey

[71]: #widgetloaded

[72]: #partialwidgetbuild
